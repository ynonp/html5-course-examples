(function() {
  /**
   * This file is almost empty. We call angular.module() with two
   * parameters to initialise the app.
   * Most of the app's functionality will be defined later via controllers
   * in different files.
   * See main_controller.js for an example
   */
   
  angular.module('myapp', []);
}());

